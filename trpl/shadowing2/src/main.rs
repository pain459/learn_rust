fn main() {

    let spaces = "    ";
    let spaces = spaces.len();
    println!("Total number of spaces are {spaces}");

    // println!("Trying another variation");

    // let spaces2 = "        ";
    // spaces2 = spaces2.len();
    // println!("This should throw an error.");
}